# Player

playerId (primary key)
username (varchar(16))
slogan (varchar(100))
is_commissioner (boolean)

# Event
eventId (primary key)
location (varchar(100))
date (date)
event_name (varchar(100))
num_races (integer)
current_race_num (integer)
event_wallet_per_player (currency)
race_wallet_per_player (currency)
can_rebet_winnings (boolean)
can_bet_future_race (boolean)
min_bets_per_race (integer)
max_bets_per_race (integer)
first_prize (currency)
second_prize (currency)
third_prize (currency)
min_straight_bet (currency)
min_exacta_bet (currency)
min_trifecta_bet (currency)
min_superfecta_bet (currency)
min_pick_2_bet (currency)
min_pick_3_bet (currency)
min_pick_4_bet (currency)
min_pick_5_bet (currency)
min_pick_6_bet (currency)

# Race
raceId (primary key)
eventId (foreign key)
race_resultsId (unique foreign key)
race_number (integer)
post_time (datetime)
num_horses (integer)
is_open_for_bets (bool)
is_resolved (bool)

# RaceResults
race_resultsId (primary key)
raceId (unique foreign key)
first_horse (integer)
second_horse (integer)
third_horse (integer)
fourth_horse (integer)
horse_1_win_mult (decimal)
horse_1_place_mult (decimal)
horse_1_show_mult (decimal)
horse_2_place_mult (decimal)
horse_2_show_mult (decimal)
horse_3_show_mult (decimal)
exacta_mult (decimal)
trifecta_mult (decimal)
superfecta_mult (decimal)
pick_2_mult (decimal)
pick_3_mult (decimal)
pick_4_mult (decimal)
pick_5_mult (decimal)
pick_6_mult (decimal)

# Scratches
scratchId (primary key)
raceId (foreign key)
horse_num (integer)

# Bets
betId (primary key)
playerId (foreign key)
raceId (foreign key)
eventId (foreign key)
resolution (enum: Unresolved, Cancelled, Scratched, Modified, Won, Lost)
is_valid (boolean)
invalid_reason (varchar(32))
bet_type (varchar(32))
bet_races (integer)
bet_unit (currency)
bet_cost (currency)
bet_winnings (currency)
is_boxed (boolean)
contains_wheel (boolean)
bet_horses (varchar(500))
timestamp (datetime)

# Entrants
entrantId (primary key)
playerId (foreign key)
eventId (foreign key)